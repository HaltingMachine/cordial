# Optional. `direnv allow` enters the build shell automatically; without direnv,
# `nix develop` does the same thing by hand. Neither is required — the per-distro
# package lists in CONTRIBUTING.md remain a first-class way to build this.
use flake
